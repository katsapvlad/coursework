<br><br>
<h4>Результаты по позиции `<%=@search%>`:</h4>

<br><br><table class="table table-striped">

	<thead>
		<tr>
			<th>ФИО</th>
			<th>Должность</th>
			<th>Время работы</th>
		</tr>
	</thead>

	<tbody>
		<% @doctors_search_by_name.each do |d|%>
		<tr>
			<td><%= d.name %></td>
			<td><%= d.position %></td>
			<td><%= d.worktime %></td>
		</tr>
		<% end %>
		<% @doctors_search_by_position.each do |d|%>
		<tr>
			<td><%= d.name %></td>
			<td><%= d.position %></td>
			<td><%= d.worktime %></td>
		</tr>
		<% end %>
	</tbody>
</table>